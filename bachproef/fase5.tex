%%=============================================================================
%% Evaluatie en metingen
%%=============================================================================

\chapter{\IfLanguageName{dutch}{Evaluatie en metingen}{Evaluation and measurements}}%
\label{ch:fase5}

Dit hoofdstuk beschrijft de evaluatie van NetBox Discovery (met Diode) en de bijhorende automatisering voor ontbrekende datatypes. In overeenstemming met de methodologie werden tijdens de evaluatie metingen/observaties uitgevoerd om te bepalen in welke mate de \emph{actualiteit} en de \emph{betrouwbaarheid} van netwerkdocumentatie in NetBox verbeteren na de discoveryflow.

\section{Evaluatiemethode}

De evaluatie gebeurde in een casestudy-context. Concreet werd de bestaande NetBox-inhoud voor de site `Ridefort` beschouwd als referentie (baseline, zie Hoofdstuk~\ref{ch:fase3}). Daarna werden NetBox Discovery (met Diode) en de aanvullende pipeline (hoofdstuk~\ref{ch:fase4}) ingezet om:

\begin{itemize}
  \item de NetBox-inventaris te vernieuwen op basis van wat de discovery daadwerkelijk ziet;
  \item na discovery meteen te verifiëren welke velden/relaties afwijken van wat eerder gedocumenteerd was;
  \item aan de hand van die discrepanties de belangrijkste effectindicatoren te operationaliseren.
\end{itemize}

Een belangrijke observatie bij deze evaluatie is dat de impact niet enkel achteraf merkbaar is: na het ontdekken van de apparaten kon je direct in NetBox zien dat delen van de vorige documentatie niet overeenkwamen met de werkelijke toestand.

\section{Indicatoren en metingen}

De evaluatie focust op indicatoren uit de methodologie (actualiteit, foutgraad, resterende correcties/effort). In dit hoofdstuk worden de volgende indicatoren uitgewerkt:

\begin{itemize}
  \item \textbf{Detectie van verouderde/incorrecte velden:} name/type en (voor zover mogelijk via discovery) device- en interface-informatie die niet overeenstemt met de situatie na discovery.
  \item \textbf{Detectie van incompleet gedocumenteerde relaties:} vooral kabelrelaties die in NetBox ontbreken of foutief zijn.
  \item \textbf{Resterende beperkingen:} datatypes die via de Orb/NetBox discoveryketen niet automatisch als relaties landen (bv. LLDP-neighbors/kabels).
  \item \textbf{Effort reductie (kwalitatief):} de mate waarin discovery het correctiewerk ondersteunt doordat fouten sneller worden blootgelegd.
\end{itemize}

\subsection{Meetdefinities}

Om interpretatieverschillen te vermijden, zijn de belangrijkste indicatoren als volgt geïnterpreteerd:
\begin{itemize}
  \item \textbf{Correcte AP-naam:} de NetBox-naam stemt overeen met de actuele naming in de site.
  \item \textbf{Correct AP-type:} het toegewezen device type in NetBox stemt overeen met het effectief geplaatste toesteltype.
  \item \textbf{Correcte kabelrelatie:} de kabelverbinding in NetBox komt overeen met de LLDP-observatie op de switch.
  \item \textbf{Resterende beperking:} een relevant dataveld kan niet automatisch via de gebruikte discoveryketen aangeleverd worden.
\end{itemize}

Deze definities werden consequent toegepast op baseline en eindtoestand in de casus `Ridefort`.

\section{Evaluatie-observatie op site `Ridefort`}

In de casus `Ridefort` was de bestaande NetBox-documentatie uit 2020 opgebouwd en werd in 2022 een access point-vervanging uitgevoerd. Bij de evaluatie werd vervolgens vastgesteld dat de fouten/inconsistenties in de vorige documentatie niet verborgen bleven: \textbf{na het ontdekken van de apparaten kon direct in NetBox gezien worden dat er afwijkingen waren}. Deze afwijkingen vielen in dezelfde categorieën als in de baseline:

\begin{itemize}
  \item \textbf{AP-namen:} een deel van de AP's had niet de correcte naam (veroudering/incorrecte attributen).
  \item \textbf{AP-types:} een deel van de AP's had niet het correcte type na de infrastructuurwijziging.
  \item \textbf{Kabelrelaties:} kabelrelaties naar de switch waren incompleet; slechts een subset van de verwachte kabelverbindingen was correct aanwezig.
 \end{itemize}

Deze directe zichtbaarheid vormt een meetbaar effect in de context van \emph{actualiteit}: het discovery-proces maakt inconsistenties sneller zichtbaar en vermindert daardoor de tijd die nodig is om te detecteren waar de documentatie afwijkt van de realiteit.

\section{Rol van de aanvullende automatisering (LLDP/kabels)}

Uit het evaluatiegedrag volgt dat NetBox Discovery/Orb vooral effectief is voor inventaris en een deel van de documentatie-updates. Voor relaties die afhankelijk zijn van LLDP-neighbors (en die via de Orb-keten niet automatisch als NetBox-relaties terechtkwamen), werd de aanvullende pipeline ingezet:

\begin{itemize}
  \item voor ondersteunde apparaten kon LLDP-informatie via de \texttt{NetworkTool}-applicatie en scripts worden opgehaald;
  \item met die LLDP-neighbors kon de pipeline kabelobjecten in NetBox aanmaken via de NetBox API.
\end{itemize}

Voor apparaten waar geen passende drivers bestaan (bv. Ruckus access points in jouw scope) bleef de aanvulling beperkt tot wat via SNMP discovery of andere haalbare stappen mogelijk was.

\section{Resultaat na NetBox Discovery en NetworkTool (Ridefort)}

Voorafgaand aan NetBox Discovery zijn alle oude AP-records/devices verwijderd, zodat er geen duplicaten konden ontstaan. Na het uitvoeren van NetBox Discovery stonden de \textbf{AP-namen} bijgevolg allemaal correct.

In de baseline waren slechts \textbf{3 van de 6} kabelrelaties van de AP's naar de switch correct gedocumenteerd. Na discovery (en de LLDP-gebaseerde verwerking via jouw pipeline) stonden de kabelrelaties \textbf{allemaal correct}. Deze correctheid werd gevalideerd op basis van de LLDP-informatie van de switch.

Een resterende beperking betreft het \textbf{serienummer} van de access points: dit lukte niet automatisch via SNMP, waardoor de AP's niet volledig op serieniveau konden worden geverifieerd/gestructureerd.

\section{Overzicht voor/na (Ridefort)}

\begin{table}[H]
  \centering
  \caption{Samenvatting van de belangrijkste voor/na resultaten voor de site `Ridefort`}
  \label{tab:ridefort-voor-na}
  \begin{tabular}{p{0.46\linewidth} p{0.22\linewidth} p{0.22\linewidth}}
    \toprule
    \textbf{Indicator} & \textbf{Baseline} & \textbf{Na discovery + pipeline} \\
    \midrule
    Correcte AP-namen in NetBox & gedeeltelijk correct & 6/6 correct \\
    Correcte AP-types in NetBox & 5/6 correct & correct (actueel na discovery) \\
    Kabelrelaties AP $\rightarrow$ switch & 3/6 correct & 6/6 correct (LLDP switch) \\
    Serienummers AP's via SNMP & niet beschikbaar & niet beschikbaar \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Extra observatie: site `Deleers Go4tech`}

Naast `Ridefort` werd ook een grotere site ontdekt: `Deleers Go4tech`. Deze site omvat 11 switches, 1 router, 1 Fortinet firewall en 48 access points. De bestaande documentatie in NetBox was ongeveer twee maanden oud. Desondanks werd bij het opnieuw uitvoeren van discovery vastgesteld dat \textbf{3 switches} een \textbf{verkeerd type} hadden in NetBox.

Deze observatie is belangrijk omdat ze aantoont dat documentatie zelfs wanneer ze recent is opgebouwd, alsnog fouten kan bevatten. Door apparaten periodiek opnieuw te ontdekken worden afwijkingen sneller zichtbaar en wordt de \emph{menselijke factor} (vergeten updates, foutieve selectie van types) gedeeltelijk uit de documentatiestroom gehaald.

\section{Kwantificatie (context)}

Voor een volledige kwantitatieve voor-na vergelijking is bijkomende vastlegging nodig (bv. exacte aantallen velden en aantal gecorrigeerde relaties). In dit hoofdstuk ligt de focus daarom op de casus-specifieke effecten die je kon vaststellen in NetBox tijdens de evaluatie: (1) directe detectie van inconsistenties na discovery, (2) volledige correctie van kabelrelaties na discovery + \texttt{NetworkTool}, en (3) resterende beperking op serienummer-informatie voor AP's.

De resultaten ondersteunen daarmee een \textbf{duidelijke richtinggevende conclusie} over effectiviteit binnen deze scope, maar geen statistische generalisatie naar alle sites of alle vendormixen zonder bijkomende metingen op grotere schaal.

